// 全局配置信息，避免git上传
ext {
    // android
    BUILD_SDK_VERSION = 23
    BUILD_TOOLS_VERSION = "25.0.2"

    // build config
    MIN_SDK_VERSION = 16
    TARGET_SDK_VERSION = 23
    VERSION_CODDE = 1
    VERSION_NAME = "1.0"
}